/**
 * {{pkg.description}}
 * @module {{pkg.name}}
 * @version {{pkg.version}}
 */

'use strict'

const amap = require('./amap')

let lib = amap.bind(this)

Object.assign(lib, amap, {
  amap
})

module.exports = lib
